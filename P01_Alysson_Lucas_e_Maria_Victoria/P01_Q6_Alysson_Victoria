		;		ES422	- MICROCOMPUTADORES - 2026.1
		;		Pr�tica 1	- Sexta quest�o (Uso de algoritmos cl�ssicos).
		;		Autores:	Alysson Lucas Pontes Cavalcante da Silva e Maria Vict�ria Martins Neves.
		
		;		INSTRUÇÕES UTILIZADAS
		;		'LDR' (Load Register) significa Carregar Registrador
		;		- É uma função permitida no visUAL para o assembly
		;		- Entretanto, precisa acessar a memória, que não é o objetivo desta prática
		;		- Utilizada apenas para armazenar o endereço de memória
		;		'MOV' (Move) siginifica Mover
		;		- Permite carregar diretamente valores pequenos nos registradores
		;		- Executa em 1 ciclo de máquina, pois não precisa acessar a RAM
		;		'STR' (Store Register) significa Armazenar Registrador
		;		- Move dados de um registrador para um endereço de memória específico
		;		'ADD' opera a adição
		;		- O primeiro termo recebe a soma entre o segundo e o terceiro
		;		'SUB' opera a subtração
		;		- O primeiro termo recebe a subtração do terceiro pelo segundo
		;		'CMP' faz uma comparação
		;		'BLT' (Branch if Less Than) significa Desvie se Menor Que
		;		- Se atendida a condição, desvia o código para o rótulo indicado
		
		;		DIVIS�O EUCLIDIANA
		;		d(i) = d(v) * q + r
		;		Divis�o por subtra��es sucessivas
		
		;		--- DEFINI��O DOS TERMOS ---
		MOV		R0, #17		; Dividendo
		MOV		R1, #5		; Divisor
		MOV		R2, #0		; Quociente (contador de divis�es, come�a zerado)
		LDR		R13, =0x100	; Endere�o base de mem�ria
		
divisao	;		Cria um rótulo chamado "divisao"
		
		;		--- CONDI��O DE PARADA ---
		CMP		R0, R1		; Verifica se o Dividendo atual � menor que o Divisor
		BLT		fim			; Se for menor, encerra a divis�o
		
		;		--- SUBTRA��ES SUCESSIVAS ---
		SUB		R0, R0, R1	; Subtrai o Divisor do Dividendo
		ADD		R2, R2, #1	; Aumenta 1 nosso Quociente (contador)
		B		divisao		; Reinicia a divis�o
		
fim		;		Cria um rótulo chamado "fim"
		
		;		--- REGISTRO DO RESULTADO NA MEM�RIA ---
		STR		R2, [R13]		; Salva o Quociente (R2) no endere�o 0x100
		ADD		R13, R13, #4	; Avan�a o ponteiro da mem�ria em 4 bytes (0x104)
		STR		R0, [R13]		; Salva o Resto (R0) no endere�o 0x104
